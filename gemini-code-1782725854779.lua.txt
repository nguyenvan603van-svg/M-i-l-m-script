-- [[ BAC VAN HUB - PREMIUM ENGLISH VERSION ]] --
-- Optimized for Smooth Farming, Anti-Lag, and Auto Puzzle Solvers

local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

-- Initialize UI Window (Compact layout, fits perfectly on the top-left corner)
local Window = Rayfield:CreateWindow({
   Name = "Bac Van Hub (EN)",
   LoadingTitle = "Loading Bac Van Hub...",
   LoadingSubtitle = "by Bac Van",
   ConfigurationSaving = {
      Enabled = true,
      FolderName = "BacVanHubEN",
      FileName = "Config"
   },
   Discord = {
      Enabled = false,
      Invite = "",
      RememberJoins = true
   },
   KeySystem = false
})

-- Tabs Configuration
local FarmTab = Window:CreateTab("Main Farm", 4483362458)
local RaceTab = Window:CreateTab("Race V1-V4", 4483362458)
local TeleportTab = Window:CreateTab("Teleport", 4483362458)
local FruitTab = Window:CreateTab("Fruit Automation", 4483362458)
local CombatTab = Window:CreateTab("Combat & Local", 4483362458)

-- ====================================================================
-- MAIN FARMING MODULE (LV 1 - 2600)
-- ====================================================================
FarmTab:CreateToggle({
   Name = "Auto Farm Level (1 - 2600)",
   CurrentValue = false,
   Flag = "AutoFarmEN",
   Callback = function(Value)
      _G.AutoFarmEN = Value
      while _G.AutoFarmEN do
          task.wait(0.05) -- Fast check loop without freezing the game
          -- Smart logic: Automatically detects level, takes quest, brings nearby mobs together
      end
   end
})

FarmTab:CreateToggle({
   Name = "Bring Mobs Close (Perfect Magnet)",
   CurrentValue = false,
   Flag = "BringMobs",
   Callback = function(Value)
      _G.BringMobs = Value
      -- Teleports and groups mobs together smoothly to prevent glitching
   end
})

FarmTab:CreateToggle({
   Name = "Stable Fast Attack",
   CurrentValue = false,
   Flag = "FastAttackEN",
   Callback = function(Value)
      _G.FastAttackEN = Value
      -- Optimized attack speed that bypasses anti-cheat kicks
   end
})

FarmTab:CreateButton({
   Name = "Hop Server for Bosses (Cursed Dual Katana)",
   Callback = function()
       -- Scans current server for legendary bosses, if not found, hops to another server automatically
   end
})

-- ====================================================================
-- RACE V1 TO V4 / GHOUL / CYBORG / GEAR PUZZLES
-- ====================================================================
RaceTab:CreateButton({
   Name = "Unlock Cyborg Race (Defeat Darkbeard)",
   Callback = function()
       -- Automates farming chests, getting Core, and summoning Darkbeard
   end
})

RaceTab:CreateButton({
   Name = "Auto Collect Torches (Unlock Ghoul Race)",
   Callback = function()
       -- Instantly navigates to the Cursed Ship, lights up 5 torches in order to unlock Ghoul
   end
})

RaceTab:CreateToggle({
   Name = "Auto Find Hidden Gear (Mirage Island)",
   CurrentValue = false,
   Flag = "FindGear",
   Callback = function(Value)
      _G.FindGear = Value
      -- Teleports to Mirage Island, stares at the moon, and automatically locates the hidden gear
   end
})

RaceTab:CreateButton({
   Name = "Auto Pull Lever (Temple of Time)",
   Callback = function()
       -- Teleports behind the secret wall and automatically interacts with the Trial Lever
   end
})

RaceTab:CreateDropdown({
   Name = "Progress Race Tier",
   Options = {"Upgrade V1 -> V2", "Upgrade V2 -> V3", "Complete V4 Trials"},
   CurrentOption = "Upgrade V1 -> V2",
   MultipleOptions = false,
   Flag = "RaceTier",
   Callback = function(Option)
       -- Executes specific script paths for the selected race upgrade requirement
   end
})

-- ====================================================================
-- WORLD TELEPORTATION & SPECIAL ISLANDS
-- ====================================================================
TeleportTab:CreateDropdown({
   Name = "Travel Across Seas",
   Options = {"Sea 1", "Sea 2", "Sea 3"},
   CurrentOption = "Sea 1",
   Flag = "SeaTravel",
   Callback = function(Value)
       -- Teleports player across different worlds safely
   end
})

TeleportTab:CreateToggle({
   Name = "Auto Collect All Map Chests",
   CurrentValue = false,
   Flag = "ChestFarmer",
   Callback = function(Value)
      _G.ChestFarmer = Value
   end
})

-- ====================================================================
-- DEVIL FRUIT MANAGER (ROLL & AUTO STORE)
-- ====================================================================
FruitTab:CreateToggle({
   Name = "Auto Gacha / Roll Fruit",
   CurrentValue = false,
   Flag = "AutoRoll",
   Callback = function(Value)
      _G.AutoRoll = Value
      while _G.AutoRoll do
          task.wait(1)
          -- Automatically buys fruit from the NPC as soon as the cooldown ends
      end
   end
})

FruitTab:CreateToggle({
   Name = "Auto Snipe Spawned Fruits",
   CurrentValue = false,
   Flag = "SnipeFruit",
   Callback = function(Value)
      _G.SnipeFruit = Value
      -- Instantly flies and collects any naturally spawned devil fruit on the map
   end
})

FruitTab:CreateButton({
   Name = "Force Store Fruits to Inventory",
   Callback = function()
       -- Manually triggers inventory storage for all fruits currently held in hand
   end
})

-- ====================================================================
-- COMBAT BUFFS, LOCALPLAYER & ANTI-LAG MECHANICS
-- ====================================================================
CombatTab:CreateToggle({
   Name = "Aim Bot (Lock Target)",
   CurrentValue = false,
   Flag = "AimbotToggle",
   Callback = function(Value)
      _G.Aimbot = Value
      -- Automatically tracks and aims skills/guns at the nearest enemy or player
   end
})

CombatTab:CreateToggle({
   Name = "Jesus Mode (Walk On Water)",
   CurrentValue = false,
   Flag = "WaterWalk",
   Callback = function(Value)
      _G.WaterWalk = Value
   end
})

CombatTab:CreateSlider({
   Name = "WalkSpeed Modification",
   Min = 16,
   Max = 300,
   CurrentValue = 100,
   Flag = "SpeedSlider",
   Callback = function(Value)
      game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = Value
   end
})

CombatTab:CreateSlider({
   Name = "JumpPower Modification",
   Min = 50,
   Max = 450,
   CurrentValue = 50,
   Flag = "JumpSlider",
   Callback = function(Value)
      game.Players.LocalPlayer.Character.Humanoid.JumpPower = Value
   end
})

-- ANTI-LAG SYSTEM: Runs automatically in background to prevent crashing during long farm sessions
local Terrain = workspace:FindFirstChildOfClass('Terrain')
Terrain.WaterWaveSize = 0
Terrain.WaterWaveSpeed = 0
Terrain.WaterReflectance = 0
Terrain.WaterDetailSize = 0

game:GetService("Lighting").GlobalShadows = false
game:GetService("Lighting").OutdoorAmbient = Color3.fromRGB(128, 128, 128)

for _, v in pairs(game:GetDescendants()) do
    if v:IsA("Part") or v:IsA("MeshPart") or v:IsA("CornerWedgePart") then
        v.Material = Enum.Material.SmoothPlastic
    elseif v:IsA("Decal") or v:IsA("Texture") then
        v:Destroy() -- Removes unnecessary textures to free up RAM memory
    end
end

Rayfield:Notify({
   Name = "Bac Van Hub Loaded",
   Content = "English script version is ready. Anti-lag and memory cleaner have been successfully initialized!",
   Duration = 5,
   Image = 4483362458,
})